algorismに3style表記で手順書くとセットアップが吐き出されるよ。
ちなみに、正手順、逆手順双方吐き出されるのでどっちも練習できるよ。

./setuper run [件数] 
件数分だけ表示。
1 <= 件数 <= algorism.txtの件数

./setuper lock [lock_id]
run時に表示される番号をlock.txtに書き込み以後そこにあるものはrunからは排除されます。
